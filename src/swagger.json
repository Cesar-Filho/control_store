{
  "openapi": "3.0.0",
  "info": {
    "title": "API do sistema de controle de loja.",
    "description": "Todas funções disponiveis no backend para uso do sistema.",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "http://localhost:3000/v1",
      "description": "API de desenvolvimento"
    }
  ],
  "paths": {
    "/forecast": {
      "get": {
        "description": "forecast api functional",
        "responses": {
          "200": {
            "description": "The list of forecast has been returned",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Success"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "ok": {
        "type": "string",
        "example": "ok"
      },
      "Success": {
        "type": "object",
        "properties": {
          "status": {
            "$ref": "#/components/schemas/ok"
          }
        }
      },
      "Error": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string"
          },
          "code": {
            "type": "number"
          },
          "error": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "documentation": {
            "type": "string"
          }
        }
      }
    }
  }
}
